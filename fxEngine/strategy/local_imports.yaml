logger: |
    from atakama_api.utils import RealLogger
    log = RealLogger('{id}')
       
ordermanager: |
    from atakama_api.orders import OrderManager
    order = OrderManager('{id}') 

simulationdate : |
    from atakama_api.dates import Date
    date= Date()
    def set_clock(clock, _id):
        date.clock = clock
        order.strategy_id = _id
        log.clock = clock
